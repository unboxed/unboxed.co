---
---

ul
  li
    = link_to 'All', '/index.html'
  li
    = link_to 'People', '/people.html'
  - data.tags.each do |tag|
    li
      = link_to tag.display_name, "/#{tag.url}.html"

br

ul
- sitemap.where(query).order_by(date: :desc).all.each_with_index do |resource, index|
  li #{index}: #{link_to resource.data.title, resource} - #{resource.data.date}
  ul
  - resource.data.tags.each do |tag|
    li style="padding-left:15px;" #{tag}

